// log
import { logger, logger_app, logger_sql } from "#src/core/logger.mjs";

export const func_config = {
  list: {
    user: {
      signup: {
        gift_all_open: true,
        gift_all_money: 50.00,
      }
    }
  },
};
